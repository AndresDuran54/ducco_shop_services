name: Deployment

concurrency: production

on:
    push: 
        branches: 
            - main-gh-actions

jobs:
    deployment:
        runs-on: ubuntu-latest
        environment: production
        steps:
            - name: deploy
              run: ls
            