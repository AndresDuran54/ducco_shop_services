name: Deploy to AKS

run-name: ${{ github.actor }}
on: 
  push:
    branches: [ ubuntu-latest ]
jobs:
  prepare_env:
    runs-on: [ self-hosted ]
    steps:
      - name: Clone project
        uses: actions/checkout@v4
