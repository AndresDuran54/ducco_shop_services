name: learn-github-actions
run-name: ${{ github.actor }} is learning GitHub Actions
on: [ push ]
jobs:
  check-bats-version:
    runs-on: [self-hosted]
    steps:
      - uses: actions/checkout@v3
      - run: echo "Estoy en $(pwd)"
      - run: ls -la
      - run: chmod 777 ./test.sh
      - run: ls -la
      - run: echo "Soy el usuario $USER"
      - run: ./test.sh
