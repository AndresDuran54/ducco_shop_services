name: Deploy to AKS

run-name: ${{ github.actor }}
on: 
  push:
    branches: [ main-gh-actions ]
jobs:
  test_sonarqube:
    runs-on: [self-hosted]
    steps:
      - name: Clone project
        uses: actions/checkout@v4
